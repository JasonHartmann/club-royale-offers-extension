[
  {
    "name": "web_search",
    "description": "Perform a web search to retrieve information from the internet.",
    "parameters": {
      "type": "object",
      "properties": {
        "query": {
          "type": "string",
          "description": "The search query string."
        }
      },
      "required": ["query"]
    }
  },
  {
    "name": "bash_shell",
    "description": "Execute shell commands in the terminal. Uses PowerShell on Windows.",
    "parameters": {
      "type": "object",
      "properties": {
        "command": {
          "type": "string",
          "description": "The command to execute."
        },
        "timeout": {
          "type": "integer",
          "description": "Timeout in seconds (optional)."
        }
      },
      "required": ["command"]
    }
  }
]